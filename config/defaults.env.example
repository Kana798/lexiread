# 内置默认凭证模板
#
# 复制本文件为同目录下的 `defaults.env` 并填入自己的值：
#
#   cp config/defaults.env.example config/defaults.env
#
# `config/defaults.env` 已被 .gitignore 排除，不会进入 Git 仓库。
# 这两个值从网易智云控制台获取：https://ai.youdao.com/
#
# 该文件用于让**打包后的桌面版**开箱即用（不需要用户手动配置）。
# 若留空或不存在，应用仍可正常运行，只是在线翻译会退化为公共接口
# （MyMemory / Google），质量与稳定性都会下降。
#
# AI 功能（改写 / 导师 / 语法 / 测验 / 双语对照）需要 DeepSeek Key：
# 从 https://platform.deepseek.com/ 获取，填入下面这行；也可在应用
# 首跑引导里直接粘贴（会写入 %APPDATA%/LexiRead/.env，优先级更高）。
#
# 优先级：LexiRead.exe 同级的 .env  >  %APPDATA% 下的 .env  >  本文件内置默认值

DEEPSEEK_API_KEY="YOUR_DEEPSEEK_API_KEY"
YOUDAO_APP_KEY="YOUR_YOUDAO_APP_KEY"
YOUDAO_APP_SECRET="YOUR_YOUDAO_APP_SECRET"
